version: "2"
options:
  verbose: true
syncs:
  app_data:
    src: ${PWD}/wordpress
    sync_strategy: 'unison'
    sync_userid: 1000
    sync_groupid: 1000
    sync_args:
    - "-prefer newer"
    - "-ignore='Path .git'"
    - "-ignore='BelowPath .git'"
